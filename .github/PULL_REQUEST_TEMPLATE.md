<!-- 제목은 `<타입>(<scope>): [기능 ID] 제목` 형식으로 쓴다. -->
<!-- squash merge라 이 PR 제목이 main에 남는 커밋 메시지가 된다. 여러 영역이 섞이면 scope는 생략한다. -->

## 작업

- Task: <`docs/team/tasks/X-NN.md` 링크 또는 "없음">
- 계약: <`K-NN` 버전·상태와 소비자 확인 여부, 또는 "해당 없음">
- 의존: <`Depends on #NN` 또는 "없음">

## 변경 요약

<!-- 무엇을 왜 바꿨는가. 3~5줄. 커밋 목록을 그대로 옮기지 말 것 -->

-

## 소유권

<!-- OWNERSHIP.md 기준. 타인 소유 파일이 있으면 소유자와 합의한 내용을 적는다 -->

- 변경 파일이 전부 내 담당 범위 안인가: 예 / 아니오
- 아니오라면 소유자와 합의한 내용:

## 동작 변경

- [ ] 없음 (문서·검사·구조 변경만, 외부에서 본 동작 동일)
- [ ] 있음 — 무엇이 어떻게 달라지는가:

## 수행한 검사

<!-- 실제로 돌린 것만 체크하고 결과를 적는다. 코드 작성과 mock 동작은 검사가 아니다 -->

- [ ] 문서만 바뀌어 빌드 검사를 생략한다 (링크·경로·담당·일정·중복 규칙은 확인함)
- [ ] `cd backend && ./gradlew build`
- [ ] `cd frontend && npm run type-check` / `npm run build`
- [ ] `bash scripts/verify-db.sh` (DB·마이그레이션 변경 시)
- [ ] 실제 API·DB로 저장 후 재조회 확인
- [ ] 권한 경계 확인 (관찰자 쓰기, 비회원, 타 프로젝트 ID)
- [ ] 기존 마이그레이션을 수정하지 않았고, 새 번호는 A가 예약한 것을 썼다
- [ ] `.env`·토큰·개인정보·업로드 파일 내용을 PR 본문과 코드에 노출하지 않았다
- [ ] 기타:

## 수행하지 않은 검사와 사유

<!-- 안 돌린 것을 '예상 통과'로 적지 않는다. DB 미기동은 '환경 미확인'으로 표시 -->

- <없으면 "없음">

## 리뷰 요청

<!-- A 서버→B, B 서버→A, C 화면→B(서버 의미 변경 시 생산자), 계약→생산자+소비자 -->

- 검토자:
- 특히 봐줬으면 하는 지점:

## 후속 작업

<!-- 이 PR 범위 밖으로 넘기는 것. 담당과 파일 경로를 적어 상대가 자기 Task로 가져갈 수 있게 한다 -->

-

---

병합 조건: CI 통과 + 작성자 외 1명 승인 + 미해결 변경 요청 없음 + 의존 PR 병합. squash merge를 쓰고 base는 `main`이다.
