# B-12 · 업무 권한·삭제 회귀 보강

담당 손정협 / 2026-09-21 / 로컬 검증 완료, 리뷰 대기.

## 변경
`TaskAssignmentRegressionTest.java`에 9개 추가:
- 관찰자의 업무 생성·수정·상태 변경 거절 (3개)
- 비회원의 업무 생성 거절
- 존재하지 않는 담당자 지정 거절, 신규 배정·알림 없음
- 일반 팀원이 다른 팀원의 업무를 삭제할 수 없음
- 생성자가 자기 업무 삭제 가능
- 팀장은 다른 팀원의 업무 삭제 가능
- 관찰자는 자기 업무라도 삭제할 수 없음

실제 ProjectAccessChecker와 모의 Repository를 사용했다. 접근 거절 시 저장소 변경이나 알림이 발생하지 않는 범위까지 검사했다. 운영 코드와 다른 담당 파일은 수정하지 않았다.

## 검증
로컬 JDK 23, Gradle 캐시를 사용해 backend에서 실행:

```text
gradlew.bat test --tests com.blackbox.service.TaskAssignmentRegressionTest --tests com.blackbox.service.TaskDeliverableTest --tests com.blackbox.service.DeliverableServiceTest --offline
```

BUILD SUCCESSFUL in 13s. TaskAssignmentRegressionTest 18 + TaskDeliverableTest 8 + DeliverableServiceTest 9 = 35개, 실패 0·오류 0. git diff --check 통과.

HTTP 인증 필터, 실제 DB FK·롤백, 프론트는 미검증이다. 잘못된 담당자 요청 전의 기존 배정이 DB에서 보존되는지는 모의 테스트만으로 입증하지 않는다.

## 다음 작업의 조건
- B-10은 현재 K-13 DRAFT와 공통 출발 SHA 미확정으로 대기 중이다.
- 기존 단위 테스트의 추가 확대보다 다음에는 A-00 기준선과 K-13 검토를 해결하고 실제 진척 API 구현에 진입하는 것이 우선이다.
- A-01 테스트 DB 준비 후 배정 실패 롤백과 업무 삭제 FK를 통합 검증한다.
- 이번 변경은 로컬 미커밋 상태다. 기존 코드 포함 브랜치를 main에 병합하지 않는다. 앞선 문서 전용 반영과 구별한다.
