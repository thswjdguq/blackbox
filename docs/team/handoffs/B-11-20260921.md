# 손정협 · 2026-09-21 최종 작업 기록

## 완료 범위

B-00 계약 초안·회귀 조사에 이어 B-11 독립 테스트 보강을 수행했다. B-10 API는 K-13 합의와 A-00 공통 기준선 대기 상태를 유지한다. 다른 담당 소유의 운영 코드는 수정하지 않았다.

신규 TaskAssignmentRegressionTest 9개:
1. 신규 업무 생성 시 제출물·요구사항·완료 기준 연결
2. 기존 미연결 업무의 첫 연결
3. 같은 제출물 지정 시 기존 요구사항 유지
4. 요구사항 ID와 해제 플래그 동시 요청 400
5. 비회원 담당자 배정 거절
6. 관찰자 담당자 배정 거절
7. 중복 담당자 ID를 한 번만 저장
8. 빈 목록으로 담당자 해제
9. 관찰자인 요청자의 배정 변경 거절

## 검증

backend에서 로컬 JDK 23·기존 Gradle 캐시로 다음 명령을 실행했다.

```text
gradlew.bat test --tests com.blackbox.service.TaskAssignmentRegressionTest --tests com.blackbox.service.TaskDeliverableTest --tests com.blackbox.service.DeliverableServiceTest --offline
```

BUILD SUCCESSFUL, 총 26개(9+8+9), 실패 0·오류 0. 새 테스트는 실제 ProjectAccessChecker와 모의 저장소를 사용한다. DB 롤백·HTTP 인증·브라우저 검증을 대신하지 않는다. 특히 배정 실패 후 기존 담당자가 DB에서 복구되는지는 트랜잭션 통합 테스트가 필요하다.

## 최종 브랜치와 커밋 후보

브랜치 이름: `2026-09-21/테스트-B00-B11-진척계약및업무회귀보강`.
기존 문서 브랜치는 유지한다. 미커밋 변경을 옮겨 담는 커밋 작업은 수행하지 않으므로 두 브랜치가 서로 다른 작업 내용을 저장했다고 오해하지 않는다.

오늘 작업으로 선별할 파일:
- `docs/team/tasks/B-00.md`
- `docs/team/tasks/B-10.md`
- `docs/team/tasks/B-11.md`
- `docs/team/contracts/K-13-progress.md`
- `docs/team/handoffs/B-00-20260921.md`
- `docs/team/handoffs/B-11-20260921.md`
- `backend/src/test/java/com/blackbox/service/TaskAssignmentRegressionTest.java`

주의: 신규 테스트는 아직 미커밋인 기존 1단계 구현에 의존한다. 위 파일만 현재 원격 main에 올리면 컴파일이 된다고 보장할 수 없다. A-00 기준선이 반영된 뒤 재검증하고 병합한다. 계약 문서는 먼저 Draft PR로 공유할 수 있다.

최초 작업에서는 커밋·push·PR·merge를 수행하지 않았다. 이후 사용자의 커밋 요청에 따라 B-00/B-11 문서·신규 테스트와 공통 협업 문서·10주 일정 변경을 선별해 로컬 커밋한다. 기존 구현 코드와 생성 파일은 포함하지 않는다. push·PR·merge는 수행하지 않는다.

## 다음 단계

A-00 SHA 확인 → K-13 A/C 검토 → B-10 API 구현. 그 전에도 담당자 실패 롤백 등의 통합 시나리오는 설계할 수 있으나 실제 DB 검증은 A-01 환경과 함께 수행한다.
