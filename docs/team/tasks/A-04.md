# A-04 · 요구사항 충족 확인 데이터 계약(K-14) 초안

- 담당: 송승준 / 검토: 손정협(소비자), 손정효(화면)
- 상태: READY
- 기능: M1 / 산출 계약: K-14 (신규 DRAFT)
- 기준 SHA: `fbbb3cb`
- 예상: 3시간
- 산출 파일: `docs/team/contracts/K-14-requirement-assessment.md`

## 기능 ID를 M1으로 둔 이유

충족 확인 데이터는 진척 조회(M1)의 구성 요소다. CONTRACTS §3-2가 진척을 `(완료 업무 수 / 연결 업무 수)`와
`(충족한 필수 요구사항 수 / 필수 요구사항 수)` 두 갈래로 표시하라고 하고, K-13의 `met`이 바로 뒤쪽이다.
DEVELOPMENT_PLAN 2주차(G1, M1 영역)에도 "요구사항 충족 확인 계약·구현"이 A 몫으로 배치돼 있다.

M3의 "필수 조건 검증"은 확정 시점에 서버가 다시 검사하는 행위이지 충족 확인 기능 자체가 아니다(CONTRACTS §3-8).
충족 확인은 M1에서 만들고 M3가 가져다 쓴다.

## 배경

B의 [K-13 진척 계약](../contracts/K-13-progress.md)은 필수 요구사항 **충족 수를 `met: null`,
`assessmentAvailable: false`로 비워둔 상태**다. 충족 확인 기능이 없기 때문이다.
그 데이터를 제공하는 쪽은 요구사항 소유자인 A다. B가 엔티티·SQL을 먼저 만들지 않도록 A가 계약을 낸다.

## 계약에 반드시 담을 것

1. **충족의 의미** — 사람이 확인하는 행위다. 업무 DONE이나 AI 판단으로 자동 승인하지 않는다(CONTRACTS §3-1).
2. 확인자·확인 시각·해제 가능 여부.
3. **요구사항 내용이 수정되면 기존 충족 확인을 초기화할지** — 초기화한다면 그 규칙을 명시한다.
4. 권한: 누가 충족 확인을 할 수 있는가(팀장만인지 팀원 포함인지). 관찰자는 불가.
5. B가 읽을 **읽기 전용 조회 메서드 또는 API** 서명. B가 A의 서비스·DTO를 수정하지 않아도 되게 한다.
6. 응답 예시: 미지원 → 지원 전환 시 K-13의 `assessmentAvailable`이 `true`로 바뀌는 시점과 조건.
7. DB: 새 컬럼인지 별도 테이블인지, migration 번호 예약(A가 유일 배정자, V20 이후).

## 허용 파일

- 신규 `docs/team/contracts/K-14-requirement-assessment.md`
- `docs/team/CONTRACTS.md` §2 표에 K-14 행 추가(A가 공통 색인 소유)

## 금지 범위

이 Task에서 구현·migration·엔티티를 만들지 않는다. **DRAFT 합의 후 별도 구현 Task.**
K-13의 1차 구현(현재 데이터만 사용)을 지연시키지 않는다. 두 계약은 독립적으로 진행한다.

## 완료 기준

- [ ] 성공·빈 상태·권한 오류 예시 JSON 포함
- [ ] C가 예시만으로 화면을 설계할 수 있음
- [ ] B가 2차 확장 착수 조건을 알 수 있음
- [ ] 미확정 항목은 DRAFT로 남기고 기능 완료로 보고하지 않음
